---
title: "Pandas — мини-проект анализа продаж"
description: "Сквозной кейс: CSV → pivot_table → matplotlib → экспорт отчёта в Excel и PNG."
slug: /lab/Примеры/1148
related:
  - title: "Pandas — типовые операции"
    doc: lab/examples/1113
  - title: "Анализ данных — pandas, NumPy, SciPy"
    doc: encyclopedia/5-languages/5-02-python/33
  - title: "Matplotlib — графики"
    doc: encyclopedia/5-languages/5-02-python/319
  - title: "Matplotlib — графики (примеры)"
    doc: lab/examples/1112
  - title: "Excel и Google Sheets — формулы"
    doc: lab/examples/1139
  - title: "NumPy — массивы и матрицы"
    doc: lab/examples/1129
  - title: "Keras и TensorFlow — нейросети"
    doc: encyclopedia/6-ai/6-03-neyroseti/114
---

# Pandas — мини-проект анализа продаж

<div class="article-tags">
  <span class="tag tag-notrequired">НЕ ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

Сквозной пайплайн **загрузка → агрегация → визуализация → экспорт** на синтетических данных о продажах. Теория: [Pandas в Python](/encyclopedia/5-languages/5-02-python/33), [Matplotlib](/encyclopedia/5-languages/5-02-python/319). Соседние примеры: [1113 — типовые операции](/lab/Примеры/1113), [1112 — графики](/lab/Примеры/1112).

**Зависимости:** `pip install pandas matplotlib openpyxl`

---

## 1. Данные и CSV

```python
import numpy as np
import pandas as pd

np.random.seed(42)
n = 100
df = pd.DataFrame({
    "date": pd.date_range("2024-01-01", periods=n, freq="D"),
    "product": np.random.choice(["A", "B", "C"], n),
    "sales": np.random.randint(50, 500, n),
    "profit": np.random.randint(10, 150, n),
})
df.to_csv("sales.csv", sep=";", index=False)
```

---

## 2. Загрузка и сводная таблица

```python
df = pd.read_csv("sales.csv", sep=";", parse_dates=["date"])
df["month"] = df["date"].dt.to_period("M")

summary = df.pivot_table(
    values="sales",
    index="month",
    columns="product",
    aggfunc="sum",
    fill_value=0,
)
print(summary)
```

`parse_dates` сразу даёт тип datetime; `pivot_table` — аналог Excel-сводной: строки — месяцы, столбцы — товары.

---

## 3. Два графика на одном холсте

```python
import matplotlib.pyplot as plt

fig, axes = plt.subplots(1, 2, figsize=(12, 4))

for col in summary.columns:
    axes[0].plot(
        summary.index.astype(str), summary[col], marker="o", label=col
    )
axes[0].set_title("Продажи по месяцам")
axes[0].set_xlabel("Месяц")
axes[0].set_ylabel("Сумма продаж")
axes[0].legend()
axes[0].grid(alpha=0.3)
axes[0].tick_params(axis="x", rotation=45)

axes[1].hist(df["profit"], bins=15, edgecolor="black", alpha=0.7, color="orange")
axes[1].set_title("Распределение прибыли")
axes[1].set_xlabel("Прибыль")
axes[1].set_ylabel("Частота")
axes[1].grid(axis="y", alpha=0.3)

plt.tight_layout()
plt.savefig("report.png", dpi=150, bbox_inches="tight")
plt.show()
```

---

## 4. Экспорт отчёта

```python
summary.to_excel("monthly_sales.xlsx")
print("Сохранено: report.png, monthly_sales.xlsx")
```

---

## 5. Дополнительная метрика (самопроверка)

```python
df["profit_per_sale"] = df["profit"] / df["sales"]

avg_by_product = df.groupby("product")["profit_per_sale"].mean()
print(avg_by_product)

fig, ax = plt.subplots(figsize=(6, 4))
avg_by_product.plot(kind="bar", ax=ax, color=["#4caf50", "#2196f3", "#ff9800"])
ax.set_title("Средняя прибыль на одну продажу")
ax.set_ylabel("Прибыль / продажа")
ax.grid(axis="y", alpha=0.3)
plt.tight_layout()
plt.savefig("report_profit_per_sale.png", dpi=150, bbox_inches="tight")
plt.show()
```

---

## Связь с маршрутом ML

Тот же стек **Pandas + Matplotlib + NumPy** — подготовка данных перед [перцептроном](/encyclopedia/6-ai/6-03-neyroseti/2) и [Keras](/encyclopedia/6-ai/6-03-neyroseti/114): нормализация признаков, проверка распределений, экспорт отчётов для команды.
