# it-lab

[lab.spirzen.ru](https://lab.spirzen.ru) — лаборатория «Вселенная IT» (stub).

```bash
npm install && npm run dev   # :4331
```

Один repo = один домен. Monorepo `it-portals` не используется.
