---
title: "C++ — олимпиадные шаблоны"
description: "Готовые заготовки C++17 для олимпиад и Codeforces. Быстрый ввод-вывод, vector, сортировка, графы, динамическое программирование, DSU, топосорт, Fenwick, KMP и полный каркас файла."
slug: /lab/Примеры/1125
related:
  - title: "C++ — о разделе"
    doc: encyclopedia/5-languages/5-06-cpp/intro
  - title: "Алгоритмы — о разделе"
    doc: encyclopedia/4-code-dev/4-01-algoritmy/intro
  - title: "Алгоритмы на Python — ЕГЭ и олимпиадка"
    doc: lab/examples/1122
  - title: "Pascal / Free Pascal — типовые программы"
    doc: lab/examples/1140
  - title: "Алгоритмы сортировки и поиска"
    doc: encyclopedia/4-code-dev/4-01-algoritmy/2
  - title: "Графы — модели и задачи"
    doc: encyclopedia/4-code-dev/4-01-algoritmy/4
  - title: "Евклид и классические алгоритмы на числах"
    doc: encyclopedia/4-code-dev/4-01-algoritmy/43
  - title: "Управляющие конструкции C++"
    doc: encyclopedia/5-languages/5-06-cpp/13
  - title: "Справочник C++"
    doc: encyclopedia/5-languages/5-06-cpp/3
  - title: "Структуры данных — о разделе"
    doc: encyclopedia/3-data-markup/3-02-struktury-dannyh/intro
  - title: "Шаблоны"
    doc: lab/examples/2
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# C++ — олимпиадные шаблоны

<div class="article-tags">
  <span class="tag tag-notrequired">НЕ ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

Приветствую! Здесь вы наверняка найдете, что ищете. Примеры в лаборатории рассчитаны на то, что мы разбираем что-то конкретное.

Текущая статья посвящена готовым заготовкам C++17 для школьных олимпиад и Codeforces.

Поэтому за теорией по текущей теме вам — в [энциклопедию](/encyclopedia/intro).
Если ещё не погружались, то маршрут прост:

1. [Основы](/section/basics)
2. [Система и сеть](/section/system-network)
3. [Данные и разметка](/section/data-markup)
4. [Код и разработка](/section/code-dev)
5. [Языки](/section/languages)
6. [Искусственный интеллект](/section/ai)
7. [Проект](/section/project)
8. [Инфраструктура и безопасность](/section/infra-security)
9. [Спин-офф](/section/spinoff)

Обязательно пройдитесь.

А теперь приступим к нашему предмету.

<div class="callout callout--tip">
  <div class="callout-title">Теория и соседние материалы</div>

  <div class="callout-body">
  <ul>
  <li>Теория алгоритмов — [раздел "Алгоритмы"](/encyclopedia/4-code-dev/4-01-algoritmy/intro)</li>
  <li>Синтаксис C++ — [C++ — о разделе](/encyclopedia/5-languages/5-06-cpp/intro), первая программа — [Первая программа](/encyclopedia/5-languages/5-06-cpp/1002)</li>
  <li>Те же приёмы на Python — [Алгоритмы на Python — ЕГЭ и олимпиадка](/lab/Примеры/1122)</li>
  <li>Оценка скорости кода — [Big-O — шпаргалка](/lab/Примеры/1128)</li>
  <li>Код алгоритмов, написанный вручную (merge sort, BST, trie и др.) — репозиторий [TheAlgorithms/C-Plus-Plus](https://github.com/TheAlgorithms/C-Plus-Plus) и [его документация](https://TheAlgorithms.github.io/C-Plus-Plus). На контесте копируйте шаблоны из этой статьи и используйте <a href="/encyclopedia/5-languages/5-06-cpp/3">STL</a> (<code>vector</code>, <code>sort</code>, <code>map</code>).</li>
  <li>Между блоками задач — [Turtle на Python](/lab/Примеры/111)</li>
  </ul>
</div>
</div>

---
## Навигация по примерам

| Раздел | Что ищут |
|--------|----------|
| [Каркас файла](#обязательный-каркас) | `iostream`, ускорение `cin`/`cout` |
| [Стартовые шаблоны](#стартовые-шаблоны) | сумма, максимум, массив |
| [1. Быстрый ввод-вывод](#1-быстрый-ввод-вывод) | много чисел, `t` тестов |
| [2. vector и sort](#2-vector-и-sort) | сортировка, `lower_bound` |
| [3. map и set](#3-map-и-set) | частоты, `unordered_map` |
| [4. Префиксные суммы](#4-префиксные-суммы) | сумма на отрезке |
| [5. Два указателя](#5-два-указателя) | пара с суммой, палиндром |
| [6. Числа](#6-числа) | НОД, модуль, быстрая степень |
| [7. Строки](#7-строки) | `string`, подстрока |
| [8. Графы](#8-графы) | список смежности, BFS, DFS |
| [9. ДП](#9-динамическое-программирование) | рюкзак, лестница |
| [10. Битмаски](#10-битмаски) | перебор подмножеств |
| [11. DSU](#11-dsu-система-непересекающихся-множеств) | компоненты связности |
| [14. Топологическая сортировка](#14-топологическая-сортировка) | порядок вершин в DAG, алгоритм Кана |
| [15. Запросы на отрезках](#15-запросы-на-отрезках) | дерево Фенвика, sparse table |
| [16. Поиск подстроки](#16-поиск-подстроки) | префикс-функция, алгоритм Кнута-Морриса-Пратта |
| [17. Каталог TheAlgorithms](#17-каталог-thealgorithms) | учебный C++ по темам энциклопедии |
| [12. Шпаргалка](#12-шпаргалка-перед-отправкой) | чек-лист |
| [13. Файл целиком](#13-файл-целиком-на-контест) | одна заготовка |

---

## Основы решения задач на C++

На олимпиаде программа почти всегда устроена одинаково:

1. **Прочитать** вход строго по формату условия.
2. **Посчитать** ответ (`vector`, сортировка, граф, ДП…).
3. **Вывести** ровно то, что просят — без лишних подписей и с правильными пробелами.

Стандартной библиотеки (`STL`) достаточно для большинства задач уровня региональной олимпиады и Div.2 Codeforces.

---

<span id="обязательный-каркас"></span>

### Обязательный каркас

Минимальный файл, который копируют в начало решения на контесте.



<ExternalCodeEmbed example="cpp/lab-1125-001" title="Обязательный каркас" minHeight={318} />



**Разбор:**

| Строка | Смысл |
|--------|-------|
| `ios::sync_with_stdio(false)` | отключает синхронизацию `cin`/`cout` с C-stdio — ускоряет ввод |
| `cin.tie(nullptr)` | `cout` не сбрасывается перед каждым `cin` |
| `using namespace std` | на контесте экономит место; в учебных проектах лучше писать `std::` явно |
| `return 0` | код завершения для ОС (на проверяющей системе не критичен) |

<div class="callout callout--warning">
  <div class="callout-title">ЕГЭ и олимпиада</div>

  <div class="callout-body">
  На ЕГЭ по информатике часто достаточно обычного <code>cin</code>. На Codeforces при <code>n = 10<sup>5</sup></code> и больше без ускорения ввода решение может получить TLE (Time Limit Exceeded). Формат вывода должен <strong>буквально</strong> совпадать с условием.
</div>
</div>

---

<span id="стартовые-шаблоны"></span>

### Стартовые шаблоны

#### Сумма n чисел



<ExternalCodeEmbed example="cpp/lab-1125-002" title="Сумма n чисел" minHeight={408} />



**Пример:**

```text
Вход:
5
3 1 4 1 5

Выход:
14
```

`long long` для суммы — чтобы не переполнить `int`, если числа до 10⁹.

---

#### Максимум в массиве

```cpp
int n;
cin >> n;
vector<int> a(n);
for (int &x : a) {
    cin >> x;
}
int mx = *max_element(a.begin a.end());
cout << mx << '\n';
```

`max_element` возвращает итератор на максимум; `*` снимает значение.

---

#### Массив из одной строки входа

Условие: одна строка из `n` целых.

```cpp
int n;
cin >> n;
vector<int> a(n);
for (int i = 0; i < n; ++i) {
    cin >> a[i];
}
```

---

## Примеры по темам

<span id="1-быстрый-ввод-вывод"></span>

### 1. Быстрый ввод-вывод

#### Несколько независимых тестов

Формат: первая строка `t`, дальше `t` задач одного типа.



<ExternalCodeEmbed example="cpp/lab-1125-003" title="Несколько независимых тестов" minHeight={336} />



Каждая итерация читает **свой** кусок входа и печатает **одну** строку ответа.

---

#### Чтение до конца файла

Редко на школьных олимпиадах, иногда в архивных задачах.

```cpp
int x;
while (cin >> x) {
    cout << x * 2 << '\n';
}
```

---

<span id="2-vector-и-sort"></span>

### 2. vector и sort

```cpp
vector<int> a = {5, 1, 4, 2};
sort(a.begin a.end());           // по возрастанию
sort(a.rbegin a.rend());         // по убыванию

// сортировка пар: сначала по первому, при равенстве — по второму
vector<pair<int, int>> p = {{1, 3}, {1, 1}, {2, 0}};
sort(p.begin p.end());

// бинарный поиск в отсортированном массиве
int pos = lower_bound(a.begin a.end 4) - a.begin();
// pos — индекс первого элемента >= 4
```

`lower_bound` работает за O(log n) только на **отсортированном** диапазоне.

---

#### k-й порядковый статистик (nth_element)

Когда нужен только k-й по величине элемент, без полной сортировки — в среднем O(n).

```cpp
vector<int> a = {5, 1, 4, 2, 3};
int k = 2;  // 0-based: третий по порядку
nth_element(a.begin a.begin() + k, a.end());
cout << a[k] << '\n';
```

---

<span id="3-map-и-set"></span>

### 3. map и set

#### Частоты элементов

```cpp
#include <map>

map<int, int> cnt;
int n;
cin >> n;
for (int i = 0; i < n; ++i) {
    int x;
    cin >> x;
    ++cnt[x];
}
for (auto [value, frequency] : cnt) {
    cout << value << ": " << frequency << '\n';
}
```

`map` хранит ключи **отсортированными** (красно-чёрное дерево), операции O(log n).

---

#### Быстрый счётчик (unordered_map)

```cpp
#include <unordered_map>

unordered_map<int, int> cnt;
// ++cnt[x];  — в среднем O(1)
```

На контесте подключайте только то, что используете — лишние `#include` не мешают, но засоряют шаблон.

---

#### Множество уникальных значений

```cpp
#include <set>

set<int> uniq;
uniq.insert(3);
uniq.insert(3);
cout << uniq.size() << '\n';  // 1

if (uniq.count(5)) {
    cout << "есть\n";
}
```

---

<span id="4-префиксные-суммы"></span>

### 4. Префиксные суммы

Сумма на отрезке `[l, r]` за O(1) после O(n) предобработки (0-based, включительно).

```cpp
vector<int> a = {2, 1, 5, 3};
int n = a.size();
vector<long long> pref(n + 1, 0);
for (int i = 0; i < n; ++i) {
    pref[i + 1] = pref[i] + a[i];
}

auto range_sum = [&](int l, int r) {
    return pref[r + 1] - pref[l];
};

cout << range_sum(1, 2) << '\n';  // a[1]+a[2] = 1+5 = 6
```

---

<span id="5-два-указателя"></span>

### 5. Два указателя

#### Пара с заданной суммой в отсортированном массиве



<ExternalCodeEmbed example="cpp/lab-1125-004" title="Пара с заданной суммой в отсортированном массиве" minHeight={336} />



---

#### Проверка палиндрома строки

```cpp
string s = "abba";
int l = 0, r = (int)s.size() - 1;
bool ok = true;
while (l < r) {
    if (s[l] != s[r]) {
        ok = false;
        break;
    }
    ++l;
    --r;
}
cout << (ok ? "YES\n" : "NO\n");
```

---

<span id="6-числа"></span>

### 6. Числа

#### НОД (алгоритм Евклида)

```cpp
long long gcd_ll(long long a, long long b) {
    while (b) {
        long long t = a % b;
        a = b;
        b = t;
    }
    return a;
}
```

В C++17 можно `std::gcd` из `<numeric>`:

```cpp
#include <numeric>
cout << gcd(12, 18) << '\n';  // 6
```

---

#### Остатки по модулю

```cpp
const int MOD = 1'000'000'007;

int add_mod(int a, int b) {
    a += b;
    if (a >= MOD) a -= MOD;
    return a;
}

int mul_mod(long long a, long long b) {
    return int((a * b) % MOD);
}
```

При сложении нескольких слагаемых удобнее сразу брать `% MOD` после каждого шага.

---

#### Быстрое возведение в степень

```cpp
long long binpow(long long a, long long n, long long mod) {
    long long res = 1;
    a %= mod;
    while (n > 0) {
        if (n & 1) {
            res = res * a % mod;
        }
        a = a * a % mod;
        n >>= 1;
    }
    return res;
}
```

---

<span id="7-строки"></span>

### 7. Строки

```cpp
#include <string>

string s;
cin >> s;                    // одно слово без пробелов
getline(cin, s);               // строка с пробелами (осторожно после cin >>)

cout << s.size() << '\n';
cout << s.substr(2, 4) << '\n'; // 4 символа с индекса 2

if (s.find("abc") != string::npos) {
    cout << "найдено\n";
}
```

Сортировка символов для проверки анаграммы:

```cpp
string a = "listen", b = "silent";
sort(a.begin a.end());
sort(b.begin b.end());
cout << (a == b ? "YES\n" : "NO\n");
```

---

<span id="8-графы"></span>

### 8. Графы

#### Список смежности (ненаправленный)

```cpp
int n = 5;  // вершины 0.n-1
vector<vector<int>> g(n);
auto add_edge = [&](int u, int v) {
    g[u].push_back(v);
    g[v].push_back(u);
};
```

---

#### BFS — кратчайшие расстояния без весов



<ExternalCodeEmbed example="cpp/lab-1125-005" title="BFS — кратчайшие расстояния без весов" minHeight={426} />



---

#### DFS — обход, острова, компоненты



<ExternalCodeEmbed example="cpp/lab-1125-006" title="DFS — обход, острова, компоненты" minHeight={354} />



---

#### BFS на сетке (лабиринт)



<ExternalCodeEmbed example="cpp/lab-1125-007" title="BFS на сетке (лабиринт)" minHeight={552} />



---

<span id="9-динамическое-программирование"></span>

### 9. Динамическое программирование

#### Подъём по лестнице (1 или 2 ступени)

```cpp
int n;
cin >> n;
vector<long long> dp(n + 1);
dp[0] = 1;
if (n >= 1) dp[1] = 1;
for (int i = 2; i <= n; ++i) {
    dp[i] = dp[i - 1] + dp[i - 2];
}
cout << dp[n] << '\n';
```

---

#### 0/1-рюкзак

`w[i]` — вес, `c[i]` — ценность, `W` — вместимость.

```cpp
int W, n;
cin >> W >> n;
vector<int> w(n), c(n);
for (int i = 0; i < n; ++i) cin >> w[i] >> c[i];

vector<int> dp(W + 1);
for (int i = 0; i < n; ++i) {
    for (int cap = W; cap >= w[i]; --cap) {
        dp[cap] = max(dp[cap], dp[cap - w[i]] + c[i]);
    }
}
cout << dp[W] << '\n';
```

Цикл по `cap` **с конца** — чтобы каждый предмет использовали не больше одного раза.

---

<span id="10-битмаски"></span>

### 10. Битмаски

Перебор всех подмножеств множества `&#123;0, …, n-1&#125;`:

```cpp
int n = 4;
vector<int> a = {10, 20, 30, 40};
for (int mask = 0; mask < (1 << n); ++mask) {
    vector<int> subset;
    for (int i = 0; i < n; ++i) {
        if (mask & (1 << i)) {
            subset.push_back(a[i]);
        }
    }
    // обработка subset
}
```

Проверка i-го бита: `mask & (1 &lt;&lt; i)`. Включить бит: `mask | (1 &lt;&lt; i)`.

---

<span id="11-dsu-система-непересекающихся-множеств"></span>

### 11. DSU (система непересекающихся множеств)

Объединение компонент, проверка, в одной ли группе две вершины, — почти за константу.



<ExternalCodeEmbed example="cpp/lab-1125-008" title="11. DSU (система непересекающихся множеств)" minHeight={444} />



---

<span id="14-топологическая-сортировка"></span>

### 14. Топологическая сортировка

**Топологическая сортировка** — список вершин ориентированного графа, в котором для каждого ребра `u → v` вершина `u` стоит **раньше** `v`. Такой порядок нужен, когда объекты связаны зависимостями.

Типичные условия задач:

- курс нельзя сдать, пока не пройдены предметы-требования;
- модуль сборки ждёт готовности других модулей;
- в расписании событие A должно произойти раньше B.

**Ориентированный граф** — рёбра имеют направление (стрелка от `u` к `v`). **DAG** (Directed Acyclic Graph, ациклический ориентированный граф) — граф **без циклов** по направлению рёбер. Топосорт определён только для DAG. Если цикл есть, упорядочить все вершины невозможно.

**Входящая степень** вершины — сколько рёбер в неё входят. В коде ниже это массив `indeg`.

**Алгоритм Кана** (Kahn's algorithm) строит порядок так:

1. В очередь кладут все вершины с входящей степенью 0.
2. Вершину из очереди добавляют в ответ и снимают исходящие рёбра (у соседей `indeg` уменьшается на 1).
3. Соседа с новым нулевым `indeg` снова кладут в очередь.
4. Если в ответе меньше `n` вершин — в графе есть цикл.

Сложность `O(V + E)`, где `V` — число вершин, `E` — число рёбер. Та же асимптотика, что у [BFS](#8-графы) на списке смежности.

```cpp
#include <queue>

int n, m;
cin >> n >> m;
vector<vector<int>> g(n);
vector<int> indeg(n);
for (int i = 0; i < m; ++i) {
    int u, v;
    cin >> u >> v;
    --u; --v;  // если в условии вершины нумеруются с 1
    g[u].push_back(v);
    ++indeg[v];
}

queue<int> q;
for (int i = 0; i < n; ++i) {
    if (indeg[i] == 0) q.push(i);
}

vector<int> order;
while (!q.empty()) {
    int v = q.front();
    q.pop();
    order.push_back(v);
    for (int to : g[v]) {
        if (--indeg[to] == 0) q.push(to);
    }
}

if ((int)order.size() != n) {
    cout << "cycle\n";  // в условии может быть NO или -1
} else {
    for (int v : order) cout << v + 1 << ' ';  // обратно к нумерации 1..n
    cout << '\n';
}
```

**Разбор ключевых мест**

- `g[u].push_back(v)` — список смежности: из `u` можно перейти в `v`.
- `indeg[v]` растёт при каждом входящем ребре.
- `--u; --v` переводит вершины из диапазона `1..n` в `0..n-1`, если так задано в условии.

**Теория и примеры**

- [Графы — модели и задачи](/encyclopedia/4-code-dev/4-01-algoritmy/4)
- [Нотация O](/encyclopedia/4-code-dev/4-01-algoritmy/311) — оценка `O(V + E)`
- Учебный файл на C++ — [topological_sort_by_kahns_algo.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/graph/topological_sort_by_kahns_algo.cpp)

---

<span id="15-запросы-на-отрезках"></span>

### 15. Запросы на отрезках

В [разделе 4](#4-префиксные-суммы) префиксные суммы считают сумму на отрезке `[l, r]` для **неизменяемого** массива. Ниже — две структуры для **множества** запросов:

- **Fenwick** — массив меняется (точечные прибавления);
- **sparse table** — массив фиксирован после построения, нужен минимум на отрезке.

#### Дерево Фенвика (Fenwick tree, BIT)

**BIT** (Binary Indexed Tree, двоичное индексированное дерево) — компактная структура в памяти. Поддерживает за `O(log n)`:

- прибавление к одному элементу `a[i] += v`;
- сумму на префиксе `[1..i]`;
- сумму на отрезке `[l, r]` как `sum(r) - sum(l - 1)`.

В олимпиадных задачах BIT часто заменяет префиксные суммы, когда массив **меняется** между запросами.

Индексация в примере **с 1** — так короче формулы `i += i & -i`. Если в условии индексы с 0, сдвиньте их при чтении.

```cpp
struct Fenwick {
    int n;
    vector<long long> bit;

    explicit Fenwick(int n_) : n(n_), bit(n_ + 1, 0) {}

    void add(int i, long long v) {
        for (; i <= n; i += i & -i) bit[i] += v;
    }

    long long sum(int i) const {
        long long s = 0;
        for (; i > 0; i -= i & -i) s += bit[i];
        return s;
    }

    long long range_sum(int l, int r) const {
        return sum(r) - sum(l - 1);
    }
};

// массив a[1..n], m запросов: прибавить v к a[i] или сумма на [l, r]
int n, m;
cin >> n >> m;
vector<long long> a(n + 1);
Fenwick fw(n);
for (int i = 1; i <= n; ++i) {
    cin >> a[i];
    fw.add(i, a[i]);
}
while (m--) {
    int t;
    cin >> t;
    if (t == 1) {
        int i;
        long long v;
        cin >> i >> v;
        fw.add(i, v);
    } else {
        int l, r;
        cin >> l >> r;
        cout << fw.range_sum(l, r) << '\n';
    }
}
```

**Разбор**

- `i & -i` выделяет младший установленный бит — основа обхода дерева за `O(log n)`.
- `long long` в суммах снижает риск переполнения при больших `n` и значениях.

**Связанные материалы**

- [Префиксные суммы](#4-префиксные-суммы) в этой статье
- [Реализация структур данных](/encyclopedia/3-data-markup/3-02-struktury-dannyh/2) — теория массивов и деревьев
- [fenwick_tree.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/range_queries/fenwick_tree.cpp)

#### Sparse table (RMQ на статическом массиве)

**RMQ** (Range Minimum Query) — запрос минимума на отрезке `[l, r]`. **Sparse table** (разреженная таблица) подходит, когда:

- массив после построения **не меняется**;
- запросов минимума очень много.

Построение за `O(n log n)`, один запрос за `O(1)`.

```cpp
vector<vector<int>> build_sparse_table(const vector<int>& a) {
    int n = (int)a.size();
    int LOG = 1;
    while ((1 << LOG) <= n) ++LOG;
    vector<vector<int>> st(LOG, vector<int>(n));
    st[0] = a;
    for (int k = 1; k < LOG; ++k) {
        for (int i = 0; i + (1 << k) <= n; ++i) {
            st[k][i] = min(st[k - 1][i], st[k - 1][i + (1 << (k - 1))]);
        }
    }
    return st;
}

int rmq(const vector<vector<int>>& st, const vector<int>& lg, int l, int r) {
    int k = lg[r - l + 1];
    return min(st[k][l], st[k][r - (1 << k) + 1]);
}

// lg[i] = floor(log2(i)); заранее: lg[1]=0; for (int i=2..n) lg[i]=lg[i/2]+1
```

**Связанные материалы**

- [sparse_table_range_queries.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/range_queries/sparse_table_range_queries.cpp)
- [prefix_sum_array.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/range_queries/prefix_sum_array.cpp) — префиксы в том же разделе репозитория

---

<span id="16-поиск-подстроки"></span>

### 16. Поиск подстроки

**Подстрока** — непрерывный фрагмент строки. В задаче обычно даны **текст** `t` и **образец** (шаблон) `p`; нужно найти все позиции, где `p` встречается в `t`.

#### Префикс-функция (массив π)

Для строки `s` значение `pi[i]` — длина **наибольшего** собственного префикса подстроки `s[0..i]`, который одновременно является её суффиксом.

Пример для `s = "ababc"`:

- `pi[0] = 0` (у одного символа нет собственного префикса);
- у `"aba"` совпадают префикс `"a"` и суффикс `"a"` → `pi[2] = 1`;
- префикс-функция помогает при сдвиге образца и лежит в основе KMP.

```cpp
vector<int> prefix_function(const string& s) {
    int n = (int)s.size();
    vector<int> pi(n);
    for (int i = 1; i < n; ++i) {
        int j = pi[i - 1];
        while (j > 0 && s[i] != s[j]) j = pi[j - 1];
        if (s[i] == s[j]) ++j;
        pi[i] = j;
    }
    return pi;
}
```

#### Алгоритм Кнута-Морриса-Пратта (KMP)

Проходит текст один раз, сравнивая с образцом. Время `O(|t| + |p|)` вместо наивного `O(|t| · |p|)` при многократных сравнениях.

```cpp
vector<int> kmp_search(const string& t, const string& p) {
    auto pi = prefix_function(p);
    vector<int> pos;
    int j = 0;
    for (int i = 0; i < (int)t.size(); ++i) {
        while (j > 0 && t[i] != p[j]) j = pi[j - 1];
        if (t[i] == p[j]) ++j;
        if (j == (int)p.size()) {
            pos.push_back(i - j + 1);
            j = pi[j - 1];
        }
    }
    return pos;
}
```

**Когда какой приём брать**

- короткие строки, один поиск — часто хватает `s.find(sub)` из [раздела 7](#7-строки);
- длинный текст, жёсткий лимит времени (TLE), много вхождений — KMP или префикс-функция;
- теория строк в энциклопедии — [Алгоритмы сортировки и поиска](/encyclopedia/4-code-dev/4-01-algoritmy/2) (бинарный поиск по ответу часто рядом по сложности).

**Учебный код**

- [knuth_morris_pratt.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/strings/knuth_morris_pratt.cpp)
- [z_function.cpp](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/strings/z_function.cpp) — родственный приём для поиска

---

<span id="17-каталог-thealgorithms"></span>

### 17. Каталог TheAlgorithms

[TheAlgorithms/C-Plus-Plus](https://github.com/TheAlgorithms/C-Plus-Plus) — открытый репозиторий с сотнями файлов на **C++17**. Каждый файл — один алгоритм или структура данных, без сторонних библиотек. Лицензия MIT. Удобно читать **после** теории в энциклопедии, когда псевдокод из статьи хочется сопоставить с рабочим кодом.

**Как пользоваться вместе с этой статьей**

- на контесте и в тренировке Codeforces — шаблоны из разделов 1–13 выше и контейнеры [STL](/encyclopedia/5-languages/5-06-cpp/3);
- для разбора устройства merge sort, BST, trie — соответствующий `.cpp` в репозитории;
- полный список файлов — [DIRECTORY.md](https://github.com/TheAlgorithms/C-Plus-Plus/blob/master/DIRECTORY.md);
- комментарии и схемы — [документация Doxygen](https://TheAlgorithms.github.io/C-Plus-Plus).

**Темы энциклопедии и папки репозитория**

| Тема | Где в энциклопедии | Папка на GitHub |
|------|----------------------|-----------------|
| Сортировка и поиск | [глава 2](/encyclopedia/4-code-dev/4-01-algoritmy/2) | [`sorting/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/sorting), [`search/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/search) |
| Графы | [глава 4](/encyclopedia/4-code-dev/4-01-algoritmy/4), [Дейкстра](/encyclopedia/4-code-dev/4-01-algoritmy/41) | [`graph/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/graph) |
| Структуры данных | [о разделе](/encyclopedia/3-data-markup/3-02-struktury-dannyh/intro), [реализация](/encyclopedia/3-data-markup/3-02-struktury-dannyh/2) | [`data_structures/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/data_structures) |
| Числа и НОД | [Евклид](/encyclopedia/4-code-dev/4-01-algoritmy/43) | [`math/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/math) |
| Динамическое программирование | [раздел 9](#9-динамическое-программирование) | [`dynamic_programming/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/dynamic_programming) |
| Битовые трюки | [раздел 10](#10-битмаски) | [`bit_manipulation/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/bit_manipulation) |
| Запросы на отрезках | [раздел 15](#15-запросы-на-отрезках) | [`range_queries/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/range_queries) |
| Строки | [раздел 7](#7-строки), [раздел 16](#16-поиск-подстроки) | [`strings/`](https://github.com/TheAlgorithms/C-Plus-Plus/tree/master/strings) |

Файлы в репозитории пишутся для обучения. Стиль и имена переменных могут отличаться от [идиом C++](/encyclopedia/5-languages/5-06-cpp/30) в вашем проекте — сравнивайте с [ООП и STL](/encyclopedia/5-languages/5-06-cpp/14).

---

<span id="12-шпаргалка-перед-отправкой"></span>

### 12. Шпаргалка перед отправкой

| Приём | Когда | Сложность (типично) |
|-------|--------|---------------------|
| `sort` + два указателя | пары, triplets | O(n log n) |
| `map` / `unordered_map` | частоты | O(n log n) / O(n) |
| Префиксные суммы | много запросов на отрезок | O(n + q) |
| `lower_bound` | поиск в отсортированном | O(log n) |
| BFS | кратчайший путь без весов | O(V + E) |
| DFS | компоненты, острова | O(V + E) |
| Топосорт (Кан) | порядок в DAG, зависимости | O(V + E) |
| ДП | оптимум, рюкзак | зависит от таблицы |
| DSU | связность, минимальный остов (Крускал) | ≈ O(n α(n)) |
| Fenwick / sparse table | сумма или минимум на отрезке | O(log n) / O(1) на запрос |
| KMP / π | поиск подстроки в длинном тексте | O(n + m) |
| Битмаски | перебор подмножеств | O(2ⁿ · n) |

<div class="callout callout--warning">
  <div class="callout-title">Граничные случаи</div>

  <div class="callout-body">
  <ul>
  <li><code>n = 0</code> или пустой массив</li>
  <li>один элемент</li>
  <li>все числа одинаковые</li>
  <li>переполнение <code>int</code> — используйте <code>long long</code></li>
  <li>индексация с 0 или с 1 в ответе</li>
  <li>лишний пробел или перевод строки в выводе</li>
  </ul>
</div>
</div>

---

<span id="13-файл-целиком-на-контест"></span>

### 13. Файл целиком на контест

Скопируйте в начало, допишите логику в `solve()` или сразу в `main`.



<ExternalCodeEmbed example="cpp/lab-1125-009" title="13. Файл целиком на контест" minHeight={678} />



`accumulate` из `<numeric>` считает сумму диапазона.

---

## Куда двигаться дальше

| Уровень | Материал |
|---------|----------|
| Те же задачи на Python | [Алгоритмы на Python — ЕГЭ и олимпиадка](/lab/Примеры/1122) |
| Сложность O(n) | [Нотация O](/encyclopedia/4-code-dev/4-01-algoritmy/311) |
| Сортировка и поиск | [глава 2](/encyclopedia/4-code-dev/4-01-algoritmy/2) |
| Графы | [глава 4](/encyclopedia/4-code-dev/4-01-algoritmy/4) |
| Учебный C++ по темам | [раздел 17](#17-каталог-thealgorithms), [TheAlgorithms/C-Plus-Plus](https://github.com/TheAlgorithms/C-Plus-Plus) |
| STL подробнее | [Справочник C++](/encyclopedia/5-languages/5-06-cpp/3), [Работа с данными](/encyclopedia/5-languages/5-06-cpp/24) |
| Сборка локально | [CMake](/encyclopedia/5-languages/5-06-cpp/1006) |
| Игры на C++ | [Разработка игр](/encyclopedia/5-languages/5-06-cpp/22) |

---
